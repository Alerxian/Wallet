name: wallet_flutter
description: Flutter wallet project mirroring wallet-clean feature set.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  flutter_secure_storage: ^9.2.2
  qr_flutter: ^4.1.0
  intl: ^0.20.2
  webview_flutter: ^4.10.0
  local_auth: ^2.3.0
  uuid: ^4.5.1
  web3dart: ^2.7.3
  http: ^1.3.0
  bip39: ^1.0.6
  bip32: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  test: ^1.25.8

flutter:
  uses-material-design: true
