# 主题切换功能

## 概述
应用支持两种主题：深色主题（黑色）和蓝色主题（浅色）。

## 主题配置

### 深色主题（Dark Theme）
- **背景色**: 深黑色 (#0A0A0A)
- **卡片背景**: 深灰色 (#1A1A1A)
- **主色调**: 深蓝色系
- **文字颜色**: 白色/浅灰色
- **适用场景**: 夜间使用，护眼

### 蓝色主题（Blue Theme / Light Theme）
- **背景色**: 冰蓝白色 (#F0F9FF)
- **卡片背景**: 纯白色 (#FFFFFF)
- **主色调**: 天空蓝色系 (#0EA5E9)
- **文字颜色**: 深色/深蓝灰色
- **适用场景**: 日间使用，清新明亮

## 如何切换主题

1. 进入「设置」页面
2. 点击「主题」选项
3. 选择以下选项之一：
   - 🌊 蓝色主题（浅色）
   - 🌙 深色主题（黑色）
   - ⚙️ 跟随系统

## 技术实现

### 主题定义
主题配置位于 `src/theme/themes.ts`，包含：
- 主色调（primary, secondary, accent）
- 背景色（background, surface, surfaceLight）
- 文字颜色（text.primary, text.secondary, text.disabled, text.inverse）
- 状态颜色（success, warning, error, info）
- 渐变色和透明度变体

### 主题上下文
使用 React Context 管理主题状态：
```typescript
import { useTheme } from '@/theme/ThemeContext';

const { theme: colors, themeName } = useTheme();
```

### 组件适配
所有组件都应使用动态主题：
```typescript
const styles = createStyles(colors);

const createStyles = (colors: ThemeColors) => StyleSheet.create({
  container: {
    backgroundColor: colors.background,
  },
  text: {
    color: colors.text.primary,
  },
});
```

## 已适配的组件
- ✅ App.tsx（StatusBar 自动适配）
- ✅ Button（按钮文字颜色自动适配）
- ✅ Card（卡片背景自动适配）
- ✅ HomeScreen（主页完全适配）
- ✅ SettingsScreen（设置页完全适配）
- ✅ ReceiveScreen（接收页完全适配）

## 注意事项
- 按钮文字使用 `text.inverse` 确保在主色调背景上清晰可见
- StatusBar 根据主题自动切换为 `dark` 或 `light` 模式
- 二维码始终使用白色背景和黑色前景色，确保扫描兼容性
